/**
 * Placeholder for the future split WeChat shell.
 * Functions/components: WeChatLayout.
 * Dependencies: ReactNode type only.
 * Current status: not wired into App.tsx yet; active WeChat UI still lives in App.tsx.
 */
import type React from 'react';

export function WeChatLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
